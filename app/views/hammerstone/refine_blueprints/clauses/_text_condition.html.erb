<div class="mt-3 mr-3">
  <label for="<%= condition[:id] %>" class="sr-only"><%= condition[:display] %></label>
  <input
    class="rounded border border-solid border-coolGray-200 focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
    name="<%= condition[:id] %>"
    type="text"
    value="<%= input[:value] %>"
    data-action="input->refine--update#updateBlueprint"
  />
</div>
