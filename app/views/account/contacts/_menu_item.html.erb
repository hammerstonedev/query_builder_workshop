<% if can? :read, Contact.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :contacts]),
    label: t('contacts.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('contacts.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
